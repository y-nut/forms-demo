import { CustomErrorDirective } from './custom-error.directive';

describe('CustomErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomErrorDirective();
    expect(directive).toBeTruthy();
  });
});
